import styled from "styled-components";
import theme from "../../../../theme";

const ChangeImgStyled = styled.div(() => ({
    fontSize: "1.5rem",
    margin: "10px auto",
    color: theme.color.tertiary,
    cursor: "pointer",
}));

export default ChangeImgStyled;