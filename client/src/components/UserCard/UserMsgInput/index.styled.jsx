import styled from "styled-components";

const StyledUserMsgInput = styled.textarea(() => ({
    fontSize: "1rem",
    width: "80%",
    border: "none",
    borderRadius: "20px",
    outline: "none",
    padding: "10px",
    resize: "none",
}))

export default StyledUserMsgInput;

